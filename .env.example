# Shared dev defaults. Each service also has its own .env.example with
# service-specific variables (backend/.env.example, frontend/.env.local.example).
#
# For docker-compose: copy backend/.env.example to backend/.env before `docker compose up`.
