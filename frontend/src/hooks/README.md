# src/hooks

Globally reusable hooks (e.g. `useLocalStorage`, `useAuth`, `useDebounce`).

Currently empty — `useQueryBuilder` is feature-specific and lives under
`src/features/query-builder/hooks/`. Promote a hook here only when a second
feature needs it.
