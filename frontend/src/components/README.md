# src/components

Globally reusable UI primitives (Button, Input, Modal, Navbar, …).

Currently empty — every component in the app is feature-specific and lives under
`src/features/<feature>/components/`. Promote a component here only when a
second feature needs to import it.
